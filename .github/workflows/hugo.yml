name: Deploy Hugo Docs to GitHub Pages

# Stub only. The real build/deploy logic lives solely in this same file on
# master and runs here via workflow_call - do not paste the full job back
# in here. A previous version of this file WAS a full, independently
# maintained copy that silently froze at branch-creation time and never
# picked up any later change made on master (confirmed: all docs/X.Y.Z
# branches were still on a pre-runner-rename, pre-Pagefind copy), so a
# push here quietly redeployed the whole site with stale build logic and
# undid whatever had just been fixed on master. See master's
# .github/workflows/hugo.yml for the full rationale.

on:
  push:
    branches:
      - 'docs/**'
      - '!docs/*-ee-next'

jobs:
  deploy:
    uses: EximeeBPMS/eximeebpms-docs/.github/workflows/hugo.yml@master
